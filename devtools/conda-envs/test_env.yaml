name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

  - numpy
  - rdkit

    # Pip-only installs
  - pip:
    - typed-argument-parser>=1.6.1

